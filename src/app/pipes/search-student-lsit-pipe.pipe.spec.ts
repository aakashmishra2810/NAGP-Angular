import { SearchStudentLsitPipePipe } from './search-student-lsit-pipe.pipe';

describe('SearchStudentLsitPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStudentLsitPipePipe();
    expect(pipe).toBeTruthy();
  });
});
