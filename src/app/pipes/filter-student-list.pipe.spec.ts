import { FilterStudentListPipe } from './filter-student-list.pipe';

describe('FilterStudentListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStudentListPipe();
    expect(pipe).toBeTruthy();
  });
});
